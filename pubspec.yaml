name: enviro_agri_manager 
description: "Đây là ứng dụng được thiết kế để quản lý danh mục điện tử dùng chung cho ngành Nông nghiệp và Môi trường. Hệ thống cung cấp các chức năng quản lý sản phẩm, theo dõi dữ liệu môi trường, và báo cáo thống kê."
version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  http: ^1.2.2
  image_picker: ^1.1.2
  intl: ^0.20.2
  fl_chart: ^1.1.1
  google_fonts: ^6.2.1
  supabase_flutter: ^2.10.3
  uuid: ^4.5.1
  drift: ^2.28.2
  drift_flutter: ^0.2.7
  connectivity_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.28.3
  build_runner: ^2.9.0

flutter:
  uses-material-design: true
